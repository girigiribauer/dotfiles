# ---------------------------------------------------------------------------- #
# tmux に関する設定
# ---------------------------------------------------------------------------- #

# tmux の自動起動
# セッションがあればそれを利用、なければ新規で
if [ -z "$TMUX" ]
then
  if $(tmux has-session)
  then
    tmux a
  else
    tmux
  fi
fi
